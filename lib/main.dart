import 'package:flutter/material.dart';
import 'package:hive_flutter/adapters.dart';

import 'app.dart';

void main() async{
  /*await Hive.initFlutter();
  var hiveStorage = await Hive.openBox("HiveStorage");
  hiveStorage.put("key", "value");*/

  runApp(const MyApp());
}
